{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}


<div>
    <h1> Hello World </h1>

    {% if user.isConnected  %}
    
       <button>Déconnexion</button>
        {% else %}
        <button>Connexion</button>
    
    {% endif %}

   
</div>

{% endblock %}
